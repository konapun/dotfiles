require('shade').setup({
  overlay_opacity = 50,
  opacity_step = 1,
  -- TODO: Move to mappings
  keys = {
    --[[ brightness_up    = '<C-Up>',
    brightness_down  = '<C-Down>',
    toggle           = '<Leader>s', ]]
  }
})
